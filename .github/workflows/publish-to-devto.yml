name: Publish to DEV.to

on:
  push:
    branches:
      - main
    paths:
      - 'articles/**/*.md'
      - 'images/**/*.png'
      - 'images/**/*.jpg'
      - 'images/**/*.jpeg'
      - 'images/**/*.webp'
  workflow_dispatch:

jobs:
  publish:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    timeout-minutes: 30
    
    steps:
      - name: Checkout repository
        shell: bash
        env:
          GITHUB_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          HOST="${GITHUB_SERVER_URL#https://}"
          HOST="${HOST#http://}"
          REMOTE="https://x-access-token:${GITHUB_TOKEN}@${HOST}/${GITHUB_REPOSITORY}.git"

          git init .
          git remote add origin "${REMOTE}"
          git fetch --no-tags --prune --depth=2 origin "${GITHUB_REF}"
          git checkout --detach FETCH_HEAD
      
      - name: Set up Python
        shell: bash
        run: |
          set -euo pipefail
          if command -v python3.11 >/dev/null 2>&1; then
            PYTHON="python3.11"
          else
            PYTHON="python3"
          fi
          echo "PYTHON=$PYTHON" >> "$GITHUB_ENV"
          $PYTHON --version
      
      - name: Install dependencies
        run: |
          $PYTHON -m pip install requests python-frontmatter
      
      - name: Get changed files
        id: changed-files
        run: |
          # Get list of changed markdown files
          CHANGED_FILES=$(git diff --name-only HEAD~1 HEAD -- 'articles' | grep -E '\.md$' || true)
          
          # If images changed, find articles that reference those images
          CHANGED_IMAGES=$(git diff --name-only HEAD~1 HEAD -- 'images' | grep -E '\.(png|jpg|jpeg|webp)$' || true)
          if [ -n "$CHANGED_IMAGES" ]; then
            echo "Images changed: $CHANGED_IMAGES"
            # Find articles that reference the changed images
            for img in $CHANGED_IMAGES; do
              img_basename=$(basename "$img")
              # Find articles that reference this image
              matching_articles=$(grep -rl --include='*.md' "$img_basename" articles 2>/dev/null || true)
              if [ -n "$matching_articles" ]; then
                CHANGED_FILES="$CHANGED_FILES"$'\n'"$matching_articles"
              fi
            done
            # Remove duplicates and empty lines
            CHANGED_FILES=$(echo "$CHANGED_FILES" | grep -v '^$' | sort -u)
          fi
          
          if [ -z "$CHANGED_FILES" ]; then
            # If triggered manually, publish all articles
            CHANGED_FILES=$(find articles -name "*.md" 2>/dev/null || echo "")
          fi
          if [ -z "$CHANGED_FILES" ]; then
            echo "No markdown files to process"
            echo "files=" >> $GITHUB_OUTPUT
          else
            echo "files<<EOF" >> $GITHUB_OUTPUT
            echo "$CHANGED_FILES" >> $GITHUB_OUTPUT
            echo "EOF" >> $GITHUB_OUTPUT
          fi
      
      - name: Publish to DEV.to
        env:
          DEVTO_API_KEY: ${{ secrets.DEVTO_API_KEY }}
          FILES_TO_PUBLISH: ${{ steps.changed-files.outputs.files }}
          DEVTO_DELAY_SECONDS: "5"
          DEVTO_MAX_RETRIES: "6"
          DEVTO_RETRY_BASE_SLEEP_SECONDS: "2"
          DEVTO_ORG_SLUG: ${{ secrets.DEVTO_ORG_SLUG }}
          DEVTO_ORG_ID: ${{ secrets.DEVTO_ORG_ID }}
        run: |
          $PYTHON scripts/publish_to_devto.py
